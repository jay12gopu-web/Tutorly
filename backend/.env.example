GROQ_API_KEY=

# Tutorly email login. Use SMTP credentials from your mail provider.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_USE_SSL=false

# Generate a long random value (at least 24 characters). Never expose it to the browser.
TUTORLY_OTP_SECRET=

TUTORLY_ALLOWED_ORIGINS=https://mytutor.co.in,http://127.0.0.1:8000,http://127.0.0.1:8769
